import { makeStyles } from "@material-ui/core/styles";

const PreloaderStyles = makeStyles((theme) => ({
  root: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
}));

export default PreloaderStyles;
